#version 330 core

layout(location = 0)
in vec3 model_space_pos;
layout(location = 1)
in vec3 color;

out vec3 colorV;

//model view projection matrix...
uniform mat4 MVP;

void main(){
	colorV = color;
	gl_Position = MVP * vec4(model_space_pos, 1);
}